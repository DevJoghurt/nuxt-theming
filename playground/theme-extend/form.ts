import { Form} from '#theme'

export default defineTheme<Form>({
    base: {
        container: 'extend'
    },
    defaults: {
        
    },
    variants: {

    },
    props: {
        size: {
            
        }
    }
})